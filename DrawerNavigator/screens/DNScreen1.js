import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const DNScreen1 = ()=> {
  return (
    <Text>Drawer Screen 1</Text>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default DNScreen1;